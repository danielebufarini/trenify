# SQLDelight migrations

The checked-in schema under `databases/` is generated by SQLDelight. Add numbered `.sqm` files beside the `.sq` sources for every schema change after the initial release, then run `./gradlew :shared:core:database:verifySqlDelightMigration`.
